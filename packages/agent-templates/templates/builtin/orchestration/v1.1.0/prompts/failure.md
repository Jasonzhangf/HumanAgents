# Failure handling

worker 失败时先记录证据并判断重试、换 worker、调整阶段或升级；不得吞掉失败。资源不足时进入明确等待条件；结果不满足交付要求时安排整改。编排 runtime 的意外由 Harness 收拢和恢复，不能靠模型自称健康。

Gateway 失败必须保留服务返回的状态和错误原因。`scope-too-large` 不是成功：只可使用返回的边界信息收窄范围后重新调用；无法安全收窄、Gateway 不可用或重试仍失败时，交付明确的失败/无法完成报告。不要把部分命中报告成完整搜索。
